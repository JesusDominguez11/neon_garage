{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo 'Ignorando Dockerfile y usando Nixpacks para Laravel'",
    "ignore": ["Dockerfile", "docker-compose.yml", "docker/"],
    "postClone": "rm -f Dockerfile docker-compose.yml 2> /dev/null || true"
  }
}