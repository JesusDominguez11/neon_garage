<footer class="hyper-footer">
    <!-- Ola decorativa superior -->
    <div class="footer-wave">
        <svg viewBox="0 0 1200 120" preserveAspectRatio="none">
            <path d="M0,0V46.29c47.79,22.2,103.59,32.17,158,28,70.36-5.37,136.33-33.31,206.8-37.5C438.64,32.43,512.34,53.67,583,72.05c69.27,18,138.3,24.88,209.4,13.08,36.15-6,69.85-17.84,104.45-29.34C989.49,25,1113-14.29,1200,52.47V0Z" opacity=".25" fill="var(--color-primary)"></path>
            <path d="M0,0V15.81C13,36.92,27.64,56.86,47.69,72.05,99.41,111.27,165,111,224.58,91.58c31.15-10.15,60.09-26.07,89.67-39.8,40.92-19,84.73-46,130.83-49.67,36.26-2.85,70.9,9.42,98.6,31.56,31.77,25.39,62.32,62,103.63,73,40.44,10.79,81.35-6.69,119.13-24.28s75.16-39,116.92-43.05c59.73-5.85,113.28,22.88,168.9,38.84,30.2,8.66,59,6.17,87.09-7.5,22.43-10.89,48-26.93,60.65-49.24V0Z" opacity=".5" fill="var(--color-primary)"></path>
            <path d="M0,0V5.63C149.93,59,314.09,71.32,475.83,42.57c43-7.64,84.23-20.12,127.61-26.46,59-8.63,112.48,12.24,165.56,35.4C827.93,77.22,886,95.24,951.2,90c86.53-7,172.46-45.71,248.8-84.81V0Z" fill="var(--color-primary)"></path>
        </svg>
    </div>

    <!-- Contenido principal del footer -->
    <div class="footer-core">
        <!-- Sección logo con efecto 3D -->
        <div class="footer-brand">
            <div class="logo-3d">
                <span class="logo-layer" style="--i:0">MECÁNICA</span>
                <span class="logo-layer" style="--i:1">MECÁNICA</span>
                <span class="logo-layer" style="--i:2">MECÁNICA</span>
                <span class="logo-layer" style="--i:3">MECÁNICA</span>
            </div>
            <p class="tagline">Tecnología automotriz de <span>vanguardia</span></p>
            
            <!-- Redes sociales con hover magnético -->
            <div class="social-magnetic">
                <a href="#" class="social-btn" data-magnetic>
                    <i class="fab fa-whatsapp"></i>
                    <span class="tooltip">WhatsApp</span>
                </a>
                <a href="#" class="social-btn" data-magnetic>
                    <i class="fab fa-instagram"></i>
                    <span class="tooltip">Instagram</span>
                </a>
                <a href="#" class="social-btn" data-magnetic>
                    <i class="fab fa-facebook-f"></i>
                    <span class="tooltip">Facebook</span>
                </a>
                <a href="#" class="social-btn" data-magnetic>
                    <i class="fab fa-youtube"></i>
                    <span class="tooltip">YouTube</span>
                </a>
            </div>
        </div>

        <!-- Menú interactivo -->
        <div class="footer-nav">
            <h3 class="nav-title">Explora <span class="flicker">>></span></h3>
            <ul class="nav-links">
                <li><a href="#" data-cursor-hover>Inicio</a></li>
                <li><a href="#" data-cursor-hover>Servicios</a></li>
                <li><a href="#" data-cursor-hover>Taller Virtual</a></li>
                <li><a href="#" data-cursor-hover>Garantías</a></li>
                <li><a href="#" data-cursor-hover>Promociones</a></li>
                <li><a href="#" data-cursor-hover>Blog Técnico</a></li>
            </ul>
        </div>

        <!-- Horario con animación de reloj -->
        <div class="footer-hours">
            <h3 class="hours-title">Horario <i class="fas fa-clock pulse"></i></h3>
            <div class="time-card">
                <div class="time-item">
                    <div class="time-days">Lun-Vie</div>
                    <div class="time-range">8:00 - 18:00</div>
                    <div class="time-bar" style="--width: 80%"></div>
                </div>
                <div class="time-item">
                    <div class="time-days">Sábados</div>
                    <div class="time-range">9:00 - 14:00</div>
                    <div class="time-bar" style="--width: 50%"></div>
                </div>
                <div class="time-item emergency">
                    <div class="time-days">Emergencias</div>
                    <div class="time-range">24/7</div>
                    <div class="time-bar" style="--width: 100%"></div>
                </div>
            </div>
        </div>

        <!-- Contacto con efecto de chips -->
        <div class="footer-contact">
            <h3 class="contact-title">Contáctanos <i class="fas fa-comment-dots"></i></h3>
            
            <div class="contact-chip" data-cursor-hover>
                <i class="fas fa-map-marker-alt"></i>
                <span>Av. Tecnológico 1234, Tijuana</span>
                <div class="chip-tooltip">Abrir en Maps</div>
            </div>
            
            <div class="contact-chip" data-cursor-hover>
                <i class="fas fa-phone-alt"></i>
                <span>+52 664 123 4567</span>
                <div class="chip-tooltip">Llamar ahora</div>
            </div>
            
            <div class="contact-chip" data-cursor-hover>
                <i class="fas fa-envelope"></i>
                <span>contacto@mecanicaelite.com</span>
                <div class="chip-tooltip">Enviar email</div>
            </div>
            
            <!-- Formulario de newsletter -->
            <div class="newsletter">
                <h4>Recibe tips mecánicos</h4>
                <div class="newsletter-group">
                    <input type="email" placeholder="Tu email">
                    <button class="subscribe-btn">
                        <span>Suscribirse</span>
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer inferior con efecto de neón -->
    <div class="footer-bottom">
        <div class="footer-legal">
            <p>&copy; 2023 <span class="neon-text">Mecánica Premium</span>. Todos los derechos reservados.</p>
            <div class="legal-links">
                <a href="#" data-cursor-hover>Política de Privacidad</a>
                <a href="#" data-cursor-hover>Términos de Servicio</a>
                <a href="#" data-cursor-hover>Aviso Legal</a>
            </div>
        </div>
        
        <!-- Medios de pago animados -->
        <div class="payment-methods">
            <div class="payment-icon" data-tooltip="Visa">
                <i class="fab fa-cc-visa"></i>
            </div>
            <div class="payment-icon" data-tooltip="Mastercard">
                <i class="fab fa-cc-mastercard"></i>
            </div>
            <div class="payment-icon" data-tooltip="PayPal">
                <i class="fab fa-cc-paypal"></i>
            </div>
            <div class="payment-icon" data-tooltip="Bitcoin">
                <i class="fab fa-bitcoin"></i>
            </div>
            <div class="payment-icon" data-tooltip="Efectivo">
                <i class="fas fa-money-bill-wave"></i>
            </div>
        </div>
    </div>

    <!-- Elemento decorativo flotante -->
    <div class="footer-car">
        <img src="https://cdn-icons-png.flaticon.com/512/747/747310.png" alt="Car" class="floating-car">
    </div>
</footer>



































































<style>
 .hyper-footer {
    position: relative;
    background: #0a0a0a;
    color: white;
    overflow: hidden;
    font-family: 'Montserrat', sans-serif;
    --color-primary: #ff6b00;
    --color-accent: #00e1ff;
    --easing: cubic-bezier(0.5, 0, 0, 1);
}

/* Ola animada */
.footer-wave {
    position: absolute;
    top: -10px;
    left: 0;
    width: 100%;
    height: 150px;
    transform-origin: bottom;
    animation: waveAnim 12s infinite linear;
}

.footer-wave svg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

@keyframes waveAnim {
    0% { transform: scaleX(1) translateZ(0); }
    50% { transform: scaleX(1.2) translateZ(0); }
    100% { transform: scaleX(1) translateZ(0); }
}

/* Contenido principal */
.footer-core {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 3rem;
    max-width: 1400px;
    margin: 0 auto;
    padding: 5rem 2rem 3rem;
    position: relative;
    z-index: 2;
}

/* Logo 3D */
.logo-3d {
    position: relative;
    height: 80px;
    margin-bottom: 1.5rem;
    perspective: 1000px;
}

.logo-layer {
    position: absolute;
    font-size: 2.5rem;
    font-weight: 900;
    text-transform: uppercase;
    color: white;
    text-shadow: 
        0 0 10px var(--color-primary),
        0 0 20px rgba(255,107,0,0.5);
    transform: translateY(calc(var(--i) * -4px)) 
               rotateX(calc(var(--i) * -5deg)) 
               translateZ(calc(var(--i) * 10px));
    opacity: calc(1 - (var(--i) * 0.2));
    filter: blur(calc(var(--i) * 0.8px));
    animation: logoFloat 4s infinite ease-in-out alternate calc(var(--i) * 0.1s);
}

@keyframes logoFloat {
    to { transform: translateY(calc(var(--i) * -4px + 10px)) 
                   rotateX(calc(var(--i) * -5deg + 5deg)) 
                   translateZ(calc(var(--i) * 10px)); }
}

.tagline {
    font-size: 1.1rem;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;
}

.tagline span {
    position: relative;
    color: var(--color-accent);
}

.tagline span::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.6s var(--easing);
}

.tagline:hover span::after {
    transform: scaleX(1);
    transform-origin: left;
}

/* Redes sociales magnéticas */
.social-magnetic {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
}

.social-btn {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.2rem;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s var(--easing);
}

.social-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, 
        var(--color-primary), 
        var(--color-accent));
    opacity: 0;
    transition: opacity 0.3s;
    z-index: -1;
}

.social-btn:hover::before {
    opacity: 1;
}

.social-btn i {
    transition: transform 0.3s var(--easing);
}

.social-btn:hover i {
    transform: scale(1.2);
}

.tooltip {
    position: absolute;
    bottom: -30px;
    left: 50%;
    transform: translateX(-50%);
    background: #222;
    color: white;
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s;
    white-space: nowrap;
}

.social-btn:hover .tooltip {
    opacity: 1;
    visibility: visible;
    bottom: -40px;
}

/* Efecto magnético (requiere JS) */
[data-magnetic] {
    transition: transform 0.3s var(--easing);
}

/* Menú interactivo */
.nav-title {
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
}

.flicker {
    animation: flicker 2s infinite alternate;
}

@keyframes flicker {
    0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% {
        opacity: 1;
    }
    20%, 22%, 24%, 55% {
        opacity: 0.3;
    }
}

.nav-links {
    list-style: none;
    padding: 0;
    margin: 0;
}

.nav-links li {
    margin-bottom: 0.8rem;
    position: relative;
    overflow: hidden;
}

.nav-links a {
    color: white;
    text-decoration: none;
    font-size: 1.1rem;
    position: relative;
    display: inline-block;
    padding: 0.2rem 0;
}

.nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.6s var(--easing);
}

.nav-links a:hover::after {
    transform: scaleX(1);
    transform-origin: left;
}

/* Efecto cursor hover */
[data-cursor-hover] {
    cursor: pointer;
}

/* Horario animado */
.time-card {
    background: rgba(255,255,255,0.05);
    border-radius: 10px;
    padding: 1.5rem;
    backdrop-filter: blur(5px);
    border: 1px solid rgba(255,255,255,0.1);
}

.time-item {
    margin-bottom: 1.2rem;
    position: relative;
}

.time-item:last-child {
    margin-bottom: 0;
}

.time-days {
    font-weight: 600;
    margin-bottom: 0.3rem;
    color: var(--color-accent);
}

.time-range {
    font-size: 0.9rem;
    opacity: 0.8;
    margin-bottom: 0.5rem;
}

.time-bar {
    height: 4px;
    background: rgba(255,255,255,0.1);
    border-radius: 2px;
    overflow: hidden;
    position: relative;
}

.time-bar::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: calc(var(--width) * 1%);
    background: linear-gradient(90deg, 
        var(--color-primary), 
        var(--color-accent));
    animation: barFill 1.5s var(--easing) forwards;
}

@keyframes barFill {
    from { width: 0; }
    to { width: calc(var(--width) * 1%); }
}

.time-item.emergency .time-days {
    color: #ff4d4d;
}

.time-item.emergency .time-bar::after {
    background: #ff4d4d;
    animation: emergencyPulse 2s infinite alternate;
}

@keyframes emergencyPulse {
    from { opacity: 0.6; }
    to { opacity: 1; }
}

.pulse {
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
}

/* Chips de contacto */
.contact-chip {
    background: rgba(255,255,255,0.05);
    border-radius: 50px;
    padding: 0.8rem 1.2rem;
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.8rem;
    transition: all 0.3s var(--easing);
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(255,255,255,0.1);
}

.contact-chip:hover {
    background: rgba(255,255,255,0.1);
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.2);
}

.contact-chip i {
    color: var(--color-accent);
    font-size: 1.1rem;
}

.chip-tooltip {
    position: absolute;
    top: -30px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-primary);
    color: white;
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s;
    white-space: nowrap;
}

.contact-chip:hover .chip-tooltip {
    opacity: 1;
    visibility: visible;
    top: -40px;
}

/* Newsletter */
.newsletter {
    margin-top: 2rem;
}

.newsletter h4 {
    font-size: 1.1rem;
    margin-bottom: 1rem;
}

.newsletter-group {
    display: flex;
    position: relative;
}

.newsletter input {
    flex: 1;
    background: rgba(255,255,255,0.1);
    border: none;
    border-radius: 50px 0 0 50px;
    padding: 0.8rem 1.2rem;
    color: white;
    font-size: 0.9rem;
}

.newsletter input::placeholder {
    color: rgba(255,255,255,0.6);
}

.subscribe-btn {
    background: linear-gradient(45deg, 
        var(--color-primary), 
        var(--color-accent));
    border: none;
    border-radius: 0 50px 50px 0;
    color: white;
    padding: 0 1.5rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    overflow: hidden;
    position: relative;
}

.subscribe-btn span {
    position: relative;
    z-index: 2;
    transition: transform 0.3s;
}

.subscribe-btn i {
    position: relative;
    z-index: 2;
    transform: translateX(-5px);
    opacity: 0;
    transition: all 0.3s;
}

.subscribe-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, 
        var(--color-accent), 
        var(--color-primary));
    opacity: 0;
    transition: opacity 0.3s;
}

.subscribe-btn:hover span {
    transform: translateX(-5px);
}

.subscribe-btn:hover i {
    transform: translateX(0);
    opacity: 1;
}

.subscribe-btn:hover::before {
    opacity: 1;
}

/* Footer inferior */
.footer-bottom {
    position: relative;
    padding: 2rem;
    background: rgba(0,0,0,0.3);
    backdrop-filter: blur(5px);
    border-top: 1px solid rgba(255,255,255,0.1);
    z-index: 2;
}

.footer-legal {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    font-size: 0.9rem;
    color: rgba(255,255,255,0.6);
}

.neon-text {
    color: var(--color-accent);
    text-shadow: 0 0 5px var(--color-accent), 
                0 0 10px rgba(0,225,255,0.5);
    animation: neonGlow 1.5s infinite alternate;
}

@keyframes neonGlow {
    from { text-shadow: 0 0 5px var(--color-accent), 
                       0 0 10px rgba(0,225,255,0.5); }
    to { text-shadow: 0 0 10px var(--color-accent), 
                     0 0 20px rgba(0,225,255,0.8); }
}

.legal-links {
    display: flex;
    gap: 1.5rem;
}

.legal-links a {
    color: rgba(255,255,255,0.6);
    text-decoration: none;
    transition: color 0.3s;
    position: relative;
}

.legal-links a::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--color-accent);
    transition: width 0.3s var(--easing);
}

.legal-links a:hover {
    color: white;
}

.legal-links a:hover::after {
    width: 100%;
}

/* Medios de pago */
.payment-methods {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 1.5rem;
}

.payment-icon {
    font-size: 1.8rem;
    color: rgba(255,255,255,0.6);
    transition: all 0.3s;
    position: relative;
    cursor: pointer;
}

.payment-icon:hover {
    color: white;
    transform: translateY(-5px) scale(1.2);
}

.payment-icon::after {
    content: attr(data-tooltip);
    position: absolute;
    bottom: -30px;
    left: 50%;
    transform: translateX(-50%);
    background: #222;
    color: white;
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s;
    white-space: nowrap;
}

.payment-icon:hover::after {
    opacity: 1;
    visibility: visible;
    bottom: -35px;
}

/* Coche flotante */
.footer-car {
    position: absolute;
    bottom: 20px;
    right: 5%;
    z-index: 1;
    animation: carFloat 8s infinite var(--easing);
}

.floating-car {
    width: 80px;
    filter: drop-shadow(0 0 10px rgba(255,107,0,0.5));
    opacity: 0.8;
}

@keyframes carFloat {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    25% { transform: translateY(-20px) rotate(2deg); }
    50% { transform: translateY(0) rotate(0deg); }
    75% { transform: translateY(-10px) rotate(-2deg); }
}

/* Responsive */
@media (max-width: 992px) {
    .footer-core {
        grid-template-columns: 1fr 1fr;
    }
    
    .footer-car {
        display: none;
    }
}

@media (max-width: 768px) {
    .footer-core {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .footer-legal {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
    }
    
    .legal-links {
        justify-content: center;
    }
}



.custom-cursor {
    position: fixed;
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: rgba(255, 107, 0, 0.3);
    border: 2px solid var(--color-primary);
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    mix-blend-mode: difference;
    transition: 
        transform 0.2s ease,
        width 0.3s ease,
        height 0.3s ease,
        background 0.3s ease;
}

.custom-cursor.hover {
    width: 50px;
    height: 50px;
    background: rgba(255, 107, 0, 0.1);
}
</style>

<script>
// Efecto magnético para botones sociales
document.querySelectorAll('[data-magnetic]').forEach(el => {
    el.addEventListener('mousemove', (e) => {
        const rect = el.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        const centerX = rect.width / 2;
        const centerY = rect.height / 2;
        const deltaX = (x - centerX) * 0.3;
        const deltaY = (y - centerY) * 0.3;
        
        el.style.transform = `translate(${deltaX}px, ${deltaY}px)`;
    });
    
    el.addEventListener('mouseleave', () => {
        el.style.transform = 'translate(0, 0)';
    });
});

// Efecto cursor personalizado (opcional)
if (window.innerWidth > 768) {
    const cursor = document.createElement('div');
    cursor.className = 'custom-cursor';
    document.body.appendChild(cursor);
    
    document.addEventListener('mousemove', (e) => {
        cursor.style.left = `${e.clientX}px`;
        cursor.style.top = `${e.clientY}px`;
    });
    
    document.querySelectorAll('[data-cursor-hover]').forEach(el => {
        el.addEventListener('mouseenter', () => {
            cursor.classList.add('hover');
        });
        el.addEventListener('mouseleave', () => {
            cursor.classList.remove('hover');
        });
    });
}
</script>